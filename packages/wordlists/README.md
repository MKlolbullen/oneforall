# Starter Wordlists

Small development/lab wordlists used by the Sn1per-style settings pack.

These are intentionally tiny. Real production runner images should mount curated wordlists such as SecLists or an internal company-approved set.

API endpoint:

```text
GET /api/config/wordlists
```
